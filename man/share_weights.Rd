% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/share-weights.R
\name{share_weights}
\alias{share_weights}
\title{Share design weights with a linked target population}
\usage{
share_weights(
  x,
  targets,
  links,
  by,
  to,
  within,
  ...,
  multiplicity = NULL,
  target_scope = c("reached", "population")
)
}
\arguments{
\item{x}{An executed \code{tbl_sample}, the sample of the source population.}

\item{targets}{The target register: one row per target unit, in the scope
named by \code{target_scope}. Required, because a link table holds only linked
units and so cannot describe a unit that has no link of its own.}

\item{links}{The link table: one row per link between a source unit and a
target unit. Duplicate rows are refused, because a link counted twice
changes both the numerator and the denominator.}

\item{by, to}{Named character vectors matching the source sample and the
target register to \code{links}, in the same direction as a dplyr join:
\code{c(local_column = "links_column")}.}

\item{within}{The target cluster, Lavallee's \eqn{i}. Never inferred. Give
a bare column of \code{targets} for the ordinary clustered method, \code{NULL} to
make every target unit its own cluster, or \code{\link[=extend_links]{extend_links()}} to eliminate
clusters by extending the links across them.}

\item{...}{Must be empty. Arguments after it are matched by exact name.}

\item{multiplicity}{The population multiplicity \eqn{L_{ik}}{L_ik}: a bare column
of \code{targets}, or \code{\link[=complete_links]{complete_links()}} to assert that \code{links} is the
complete population register and let samplyr count it. For links carrying
an importance rather than a presence, \code{\link[=weighted_links]{weighted_links()}}.}

\item{target_scope}{What \code{targets} is. \code{"reached"}, the default, claims
only a full roster of the clusters the sample reached. \code{"population"}
asserts that it enumerates the target population, and only that form
supports checking for clusters that can never be reached.}
}
\value{
A \code{tbl_sample} whose rows are target units. \code{.weight} holds the
shared estimation weight, \code{.unit_links} the unit's population
multiplicity, and \code{.cluster_links} the cluster denominator used. Under
\code{\link[=weighted_links]{weighted_links()}} those two are \code{.link_weight} and
\code{.cluster_link_weight} instead, holding the same quantities on the
importance scale. Only the pair belonging to the scale in use is emitted.
The recorded design still describes selection from the source population.
}
\description{
\code{share_weights()} applies the generalized weight share method (Lavallee
2007): it turns a sample of one population into a weighted sample of a
second population linked to it. The rows of the result are units of the
target population, not the units that were selected.
}
\section{The weight}{

Every target unit in a target cluster reached by the sample receives the
same weight, which is the source weight carried across the links and
divided by the population number of links to that cluster:

\deqn{w_i = \sum_j \frac{I(j \in S)}{\pi_j} \frac{L_{ji}}{L_i}}{w_i = sum_j (I(j in S) / pi_j) * (L_ji / L_i)}

Assigning one weight per cluster is what makes unit-level and
cluster-level estimates of the same total agree.
}

\section{The denominator}{

\code{multiplicity} is where the statistical content sits, and it has no
default. The denominator is defined over the \emph{whole} source population,
not over the sample, so counting the rows of a link table asserts that the
table is a complete register. That assertion is the classic source of bias
in this method, so it has to be made at the call site rather than guessed:
either name a column of \code{targets} holding the population multiplicity, or
state the assertion with \code{\link[=complete_links]{complete_links()}}.

Links need not be counted. \code{\link[=weighted_links]{weighted_links()}} replaces the 0/1 indicator
with a non-negative importance, which Lavallee section 4.5 shows costs no
theory as long as each target cluster totals more than zero. Counting is
the case where every link counts for one.
}

\section{Panels and waves}{

A materialized wave is refused. Its weights carry an activation factor
whose randomization ran over the source units, and a target cluster can be
reached from source units sitting in different panels, so there is no wave
the target unit belongs to and no factor to carry. Share weights from the
master instead.

A master carrying a panel assignment is accepted, and its assignment
record does not carry into the result for the same reason: a panel is a
property of a selected source unit. The result is an ordinary shared
sample, and \verb{execute(<it>, wave = )} is refused.
}

\section{Coverage}{

A target cluster with no link to the source population can never be
reached, and the estimator understates totals by exactly its share. That is
Lavallee's Constraint 2.1, and it is detectable only when \code{targets}
enumerates the population, so it is recorded rather than warned about here
and reported at the analysis boundary.

A target unit inside a reached cluster with no link of its own is a
different thing and is correct: it receives its cluster's weight. Producing
a weight for exactly those units is one of the reasons to use this method.
}

\examples{
dwellings <- data.frame(dwelling_id = 1:20, region = rep(1:2, each = 10))
sample <- sampling_design() |>
  draw(n = 8) |>
  execute(dwellings, seed = 1)

# Two people per dwelling, each linked to the one dwelling they live in.
people <- data.frame(
  person_id = 1:40,
  household = rep(1:20, each = 2)
)
links <- data.frame(dwelling_id = rep(1:20, each = 2), person_id = 1:40)

share_weights(
  sample,
  targets = people,
  links = links,
  by = c(dwelling_id = "dwelling_id"),
  to = c(person_id = "person_id"),
  within = household,
  multiplicity = complete_links()
)

}
\references{
Lavallee, P. (2007). \emph{Indirect Sampling}. Springer.
}
\seealso{
Other weight sharing:
\code{\link[=complete_links]{complete_links()}},
\code{\link[=extend_links]{extend_links()}},
\code{\link[=weighted_links]{weighted_links()}}
}
\concept{weight sharing}
