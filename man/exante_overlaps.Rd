% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exante-probabilities.R
\name{exante_overlaps}
\alias{exante_overlaps}
\title{Resolve overlap probabilities from the designs, instead of stating them}
\usage{
exante_overlaps(frames, ..., by)
}
\arguments{
\item{frames}{A named list of registers, one per frame, with the same names
as the components of the stack. Each is the population the corresponding
design would draw from.}

\item{...}{Must be empty. Arguments after it are matched by exact name.}

\item{by}{A single named string matching the stack's key to the column
holding it in the registers, in the same direction as a join:
\code{by = c(person_id = "person_id")}.}
}
\value{
An object of class \code{samplyr_exante_overlap_spec}, for
\code{stack_frames()}'s \code{overlaps} argument.
}
\description{
A declarative marker for \code{\link[=stack_frames]{stack_frames()}}'s \code{overlaps} argument. Instead of
naming columns that already hold the chance each unit had in every frame,
it names the registers, and samplyr resolves the chances from each
component's own design with \code{\link[=exante_probabilities]{exante_probabilities()}}.

This is the capability the expected multiframe estimator needs and that a
sample alone cannot supply: the probability a unit would have had in a
frame it was not selected from. It is exact, and it is checked against what
actually happened, because a component's own resolved chance has to equal
the design weight the execution produced.
}
\examples{
exante_overlaps(
  frames = list(
    area = data.frame(person_id = 1:10),
    list = data.frame(person_id = 5:14)
  ),
  by = c(person_id = "person_id")
)

}
\seealso{
\code{\link[=exante_probabilities]{exante_probabilities()}} for one register,
\code{\link[=declared_overlaps]{declared_overlaps()}} for naming columns that already hold them

Other multiple frames:
\code{\link[=as.data.frame.frame_stack]{as.data.frame.frame_stack()}},
\code{\link[=as_svrepdesign.frame_stack]{as_svrepdesign.frame_stack()}},
\code{\link[=as_svydesign.frame_stack]{as_svydesign.frame_stack()}},
\code{\link[=declared_overlaps]{declared_overlaps()}},
\code{\link[=exante_probabilities]{exante_probabilities()}},
\code{\link[=stack_frames]{stack_frames()}},
\code{\link[=summary.frame_stack]{summary.frame_stack()}}
}
\concept{multiple frames}
