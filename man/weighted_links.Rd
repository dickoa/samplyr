% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/share-weights.R
\name{weighted_links}
\alias{weighted_links}
\title{Weight links by their importance rather than counting them}
\usage{
weighted_links(x, total)
}
\arguments{
\item{x}{A single non-negative column of \code{links} holding the importance of
each link.}

\item{total}{The population total of that importance for each target unit:
a column of \code{targets}, or \code{\link[=complete_links]{complete_links()}} to assert that \code{links} is the
complete population register and let samplyr total the importances. That
is the same assertion \code{multiplicity = complete_links()} makes about
counts, so it is the same marker.}
}
\value{
\code{weighted_links()} is only meaningful inside
\code{share_weights(multiplicity = )} and otherwise throws an error.
}
\description{
A declarative marker for \code{\link[=share_weights]{share_weights()}}'s \code{multiplicity} argument. It
replaces the 0/1 link indicator with a non-negative quantity saying how much
of a target unit each link accounts for: a subsidiary's share of an
enterprise's assets, a child's share of time with each parent.
}
\details{
Lavallee section 4.5 generalizes the method to any such quantity with no
loss of theory, subject to Constraint 4.1: the total over each target
cluster must be strictly positive.

The argument is not called \code{theta}. Lavallee uses that name for normalized
link constants and \code{survey::multiframe()} uses it for an unrelated
frame-compositing factor, and a workflow can involve both.
}
\examples{
# Used inside share_weights():
# share_weights(sample, targets, links, by, to, within = enterprise,
#               multiplicity = weighted_links(asset_share,
#                                             total = population_assets))
# or, asserting that `links` is the complete register:
# share_weights(sample, targets, links, by, to, within = enterprise,
#               multiplicity = weighted_links(asset_share,
#                                             total = complete_links()))

}
\seealso{
Other weight sharing:
\code{\link[=complete_links]{complete_links()}},
\code{\link[=extend_links]{extend_links()}},
\code{\link[=share_weights]{share_weights()}}
}
\concept{weight sharing}
