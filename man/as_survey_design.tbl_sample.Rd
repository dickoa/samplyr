% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey.R
\name{as_survey_design.tbl_sample}
\alias{as_survey_design.tbl_sample}
\title{Convert a tbl_sample to a srvyr tbl_svy object}
\usage{
\method{as_survey_design}{tbl_sample}(.data, ...)
}
\arguments{
\item{.data}{A \code{tbl_sample} object produced by \code{\link[=execute]{execute()}}.}

\item{...}{Additional arguments passed to \code{\link[=as_svydesign]{as_svydesign()}}.}
}
\value{
A \code{tbl_svy} object from the srvyr package.
}
\description{
Creates a \link[srvyr:tbl_svy]{srvyr::tbl_svy} object from a \code{tbl_sample} by first
converting to a \code{\link[survey:svydesign]{survey::svydesign()}} object via \code{\link[=as_svydesign]{as_svydesign()}},
then wrapping with \code{\link[srvyr:as_survey_design]{srvyr::as_survey_design()}}.
}
\details{
This method is registered on the \code{\link[srvyr:as_survey_design]{srvyr::as_survey_design()}} generic,
so it is available when srvyr is loaded.
}
\examples{
\dontrun{
library(srvyr)

sample <- sampling_design() |>
  stratify_by(facility_type, alloc = "proportional") |>
  draw(n = 300) |>
  execute(kenya_health, seed = 42)

# Returns a tbl_svy for use with srvyr verbs
svy <- as_survey_design(sample)
svy |>
  group_by(facility_type) |>
  summarise(mean_beds = survey_mean(beds))
}

}
\seealso{
\code{\link[=as_svydesign]{as_svydesign()}} for converting to a survey.design2 object
}
