% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{as_tbl_sample}
\alias{as_tbl_sample}
\alias{as_tbl_sample.tbl_sample}
\alias{as_tbl_sample.data.frame}
\title{Coerce to a tbl_sample}
\usage{
as_tbl_sample(x, ...)

\method{as_tbl_sample}{tbl_sample}(x, ...)

\method{as_tbl_sample}{data.frame}(x, ...)
}
\arguments{
\item{x}{A data frame with sampling attributes.}

\item{...}{Must be empty.}
}
\value{
A \code{tbl_sample}, or an error if \code{x} does not carry the
required attributes.
}
\description{
Restores the \code{tbl_sample} class on a data frame that carries sampling
attributes (\code{design}, \code{stages_executed}) but has lost its class, for
example after a tidyr operation such as \code{\link[tidyr:uncount]{tidyr::uncount()}}.
}
\section{Class preservation}{


All dplyr verbs (\code{mutate}, \code{filter}, \code{select}, \verb{*_join}, etc.)
preserve the \code{tbl_sample} class automatically.

Preserving the class does not mean the sample remains analyzable:
operations that remove, add, or duplicate rows, or overwrite
internal design columns (\code{.weight}, \code{.fpc_k}, ...), mark the sample
as modified. Design-based computations (\code{\link[=as_svydesign]{as_svydesign()}},
\code{\link[=joint_expectation]{joint_expectation()}}, \code{\link[=design_effect]{design_effect()}}) reject modified samples;
see the Domain analysis section of \code{\link[=as_svydesign]{as_svydesign()}}. Restoring the
class with \code{as_tbl_sample()} does not clear the mark: the data is
re-verified against the integrity record stored at execution, so a
stripped, altered, and restored object is detected.

Some operations strip the class but keep the sampling attributes.
Use \code{as_tbl_sample()} to restore it:
\itemize{
\item \code{tidyr::uncount()}
\item \code{tibble::as_tibble()}
\item \code{as.data.frame()}
}

For an operational multistage listing, keep the unmodified partial sample
as the first argument to \code{\link[=execute]{execute()}} and pass the expanded plain object only
as its frame; restoring the listing itself is not required. A plain object
that still carries sample provenance is rejected as the frame of a fresh
design execution, because that would rerun stage 1.

Other operations strip both class and attributes and are
not recoverable. Use dplyr alternatives instead:
\itemize{
\item \code{base::merge()} -- use \code{dplyr::left_join()} etc.
\item \code{base::cbind()} -- use \code{dplyr::bind_cols()}
\item \code{tidyr::pivot_longer()} / \code{tidyr::pivot_wider()}
}
}

\examples{
design <- sampling_design() |>
  stratify_by(region) |>
  draw(n = 20)

sample <- execute(design, bfa_eas, seed = 42)

# as_tibble() strips the class but keeps attributes
plain <- tibble::as_tibble(sample)
is_tbl_sample(plain)

# as_tbl_sample() restores it
restored <- as_tbl_sample(plain)
is_tbl_sample(restored)
}
