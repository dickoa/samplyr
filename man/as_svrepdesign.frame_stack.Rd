% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey.R
\name{as_svrepdesign.frame_stack}
\alias{as_svrepdesign.frame_stack}
\title{Export overlapping frames to a combined replicate-weight design}
\usage{
\method{as_svrepdesign}{frame_stack}(
  x,
  ...,
  estimator = c("constant", "expected"),
  theta = NULL,
  type = c("auto", "JK1", "JKn", "BRR", "bootstrap", "subbootstrap", "mrbbootstrap",
    "Fay"),
  systematic_variance = c("warn", "approximate", "error")
)
}
\arguments{
\item{x}{A \code{frame_stack} from \code{\link[=stack_frames]{stack_frames()}}.}

\item{...}{Passed to \code{\link[=as_svrepdesign]{as_svrepdesign()}} for every component and on to the
replicate-weight generator, such as \code{replicates} or \code{fay.rho}. \code{mse} is
refused.}

\item{estimator}{\code{"constant"}, the default, or \code{"expected"}, which needs
the \code{overlaps} declared on the stack. See \code{\link[=as_svydesign.frame_stack]{as_svydesign.frame_stack()}},
which documents both. Here the expected estimator takes any number of
frames, since nothing is delegated to \code{survey::multiframe()}.}

\item{theta}{The compositing factor for the first frame's overlapping
units, a single number in \verb{[0, 1]}, for two frames only. \code{NULL}, the
default, is the multiplicity estimator and works for any number.}

\item{type, systematic_variance}{Passed to \code{\link[=as_svrepdesign]{as_svrepdesign()}} for every
component.}
}
\value{
A \code{svyrep.design} object from the survey package.
}
\description{
Exports a \code{\link[=stack_frames]{stack_frames()}} collection to one \code{svyrep.design} whose
replicate columns are grouped in blocks, one block per frame. In a column
belonging to frame \code{q} only frame \code{q} varies and every other frame stays at
its full-sample weight, so the combined variance is the sum of the frames'
own contributions, which is what independent selection from each frame
gives.

Unlike the linearized route this takes any number of frames, exports a
component whose weights were shared from another population, and lets each
frame use the replicate method that suits its own design.
}
\details{
\subsection{The compositing factor}{

\code{theta = NULL} is the multiplicity estimator: a unit reached by \code{m} frames
contributes \code{1/m} of its weight through each of them. It is defined for any
number of frames and needs nothing stated.

An explicit \code{theta} is Hartley's constant factor and applies to \strong{two}
frames only, the first frame of the stack carrying \code{theta} of an
overlapping unit's weight. Above two frames the factors are per domain
rather than per frame, up to \code{2^K - 1} of them each summing to one over the
frames in that domain, so a single number is not a partial answer but a
wrong one. It is refused rather than recycled.
}

\subsection{Replicate methods may differ between frames}{

\code{type = "auto"} picks a method per component, so a PPS frame can take
\code{"subbootstrap"} beside a stratified frame taking \code{"JKn"}. Each block keeps
its own component's \code{scale} and \code{rscales}, folded together so the combined
design carries \code{scale = 1}, and mixing methods costs nothing: the blocks do
not interact.
}

\subsection{Centering}{

Every block is centered at the full combined estimate, which is what makes
the block contributions add up. \code{mse} is therefore not accepted: with
mean-centering survey would center at the mean over \emph{all} columns and mix
the blocks together.
}

\subsection{The returned data}{

The rows are the components' rows in stack order, with \code{.frame} and
\code{.domain} from \code{\link[=as.data.frame.frame_stack]{as.data.frame.frame_stack()}} in front. \code{.weight} holds the
\strong{composited} weight, so it agrees with the design's own. Every other
generated column is the component's and describes its selection alone.
}
}
\examples{
\dontshow{if (requireNamespace("survey", quietly = TRUE)) withAutoprint(\{ # examplesIf}
population <- data.frame(
  person_id = 1:200,
  spend = stats::rnorm(200, 100, 10),
  in_landline = rep(c(TRUE, FALSE), times = c(140, 60)),
  in_cell = rep(c(FALSE, TRUE), times = c(60, 140))
)

frames <- stack_frames(
  landline = sampling_design() |>
    draw(n = 40) |>
    execute(population[population$in_landline, ], seed = 1),
  cell = sampling_design() |>
    draw(n = 50) |>
    execute(population[population$in_cell, ], seed = 2),
  membership = c(landline = "in_landline", cell = "in_cell"),
  key = person_id
)

rep_svy <- as_svrepdesign(frames, type = "bootstrap", replicates = 50)
survey::svytotal(~spend, rep_svy)
\dontshow{\}) # examplesIf}
}
\references{
Lohr, S. L. (2021). Multiple-frame surveys for a multiple-data-source
world. \emph{Survey Methodology}, 47(2), 229-263.

Mecatti, F. (2007). A single frame multiplicity estimator for multiple
frame surveys. \emph{Survey Methodology}, 33(2), 151-157.
}
\seealso{
\code{\link[=as_svydesign.frame_stack]{as_svydesign.frame_stack()}} for the linearized route,
\code{\link[=stack_frames]{stack_frames()}} for building the collection

Other multiple frames:
\code{\link[=as.data.frame.frame_stack]{as.data.frame.frame_stack()}},
\code{\link[=as_svydesign.frame_stack]{as_svydesign.frame_stack()}},
\code{\link[=declared_overlaps]{declared_overlaps()}},
\code{\link[=exante_overlaps]{exante_overlaps()}},
\code{\link[=exante_probabilities]{exante_probabilities()}},
\code{\link[=stack_frames]{stack_frames()}},
\code{\link[=summary.frame_stack]{summary.frame_stack()}}
}
\concept{multiple frames}
