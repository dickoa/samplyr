% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stack-frames.R
\name{stack_frames}
\alias{stack_frames}
\title{Stack samples selected from overlapping frames}
\usage{
stack_frames(..., membership, key, overlaps = NULL)
}
\arguments{
\item{...}{Two or more named samples, one per frame, each an executed
\code{tbl_sample}. The names are the frame names and they appear in the
\code{.domain} labels, so none may contain \code{+}.}

\item{membership}{A named character vector mapping every frame name to the
column holding that frame's membership indicator, in the same direction
as \code{c(frame_name = "column_name")}. Every component must contain every
one of the columns.}

\item{key}{A bare column identifying the target-population unit, present in
every component. The same unit may be selected from several frames, so
the key repeats across components on purpose. Within a component it must
be unique, or unique per selection occurrence where the component
replicates rows with replacement.}

\item{overlaps}{Optional. The probability, or the weight, each sampled unit
\emph{would have had} in every frame, including the frames it was not selected
from. Name the columns holding them with \code{\link[=declared_overlaps]{declared_overlaps()}}, which
states the scale, since it is never inferred from the values. Or name the
registers with \code{\link[=exante_overlaps]{exante_overlaps()}} and let samplyr resolve the chances
from each component's own design, which is exact and is checked against
what the execution produced. Required by \code{estimator = "expected"} at
export and unused by the default estimator, which reads membership
alone.}
}
\value{
An object of class \code{frame_stack}: a named list of the component
samples, unchanged, carrying the membership mapping, the key, and any
overlap record. It is not a \code{tbl_sample} and not a data frame. \code{[[}
returns an intact component, and \code{\link[=as.data.frame]{as.data.frame()}} gives the row-bound
inspection view.
}
\description{
Collects two or more samples, each selected from a frame that covers part
of one target population, into a single object that records which frames
every sampled unit belongs to. Frame A and frame B are two registers of the
same people, and a unit listed in both had two chances of being selected.

The samples are selected independently: two designs, two frames, two seeds,
two calls to \code{\link[=execute]{execute()}}. Nothing about selection is shared, so nothing is
combined until here.
}
\details{
\subsection{What it does not do}{

It does not composite the weights. Which compositing factor to use, and
whether it should follow Hartley, the multiplicity estimator or
pseudo-maximum-likelihood, depends on the estimand and on the design
effects, so it is an estimation-time choice rather than a property of the
stack. The components keep their own design weights, unchanged.

It does not assert that the frames together cover the target population.
That is an assumption about the registers, not something this call can
establish, which is why the verb is not named after the union.
}

\subsection{Membership is declared, and it is logical}{

\code{membership} maps each frame's name to the column saying whether a unit
belongs to that frame. Every component must carry all of the columns, not
only its own: a unit selected from frame A has to say whether it was also
listed in frame B, and that is the information compositing needs. An
unresolved membership is an error rather than \code{NA}.

Membership columns must be \code{logical}. Integer \code{0} and \code{1} are refused
rather than read, because numeric overlap information is a different input
on a different scale.
}

\subsection{Independence is assumed, and one violation of it is detectable}{

Two components carrying the same recorded seed warn with
\code{samplyr_warning_frame_seed_reused}. Distinct seeds are not evidence of
independence, and the warning detects one recorded common-random-number
mistake and nothing more. A component executed with \code{seed = NULL} records
no seed and never warns, so a deterministic take-all component is exempt
unless it was given a seed it did not use.
}
}
\examples{
population <- data.frame(
  person_id = 1:60,
  in_landline = rep(c(TRUE, FALSE), times = c(40, 20)),
  in_cell = rep(c(FALSE, TRUE), times = c(10, 50))
)

s_landline <- sampling_design() |>
  draw(n = 10) |>
  execute(population[population$in_landline, ], seed = 1)

s_cell <- sampling_design() |>
  draw(n = 12) |>
  execute(population[population$in_cell, ], seed = 2)

frames <- stack_frames(
  landline = s_landline,
  cell = s_cell,
  membership = c(landline = "in_landline", cell = "in_cell"),
  key = person_id
)

frames
summary(frames)
head(as.data.frame(frames))

}
\references{
Lohr, S. L. (2021). Multiple-frame surveys for a multiple-data-source
world. \emph{Survey Methodology}, 47(2), 229-263.

Mecatti, F. (2007). A single frame multiplicity estimator for multiple
frame surveys. \emph{Survey Methodology}, 33(2), 151-157.
}
\seealso{
\code{\link[=as.data.frame.frame_stack]{as.data.frame.frame_stack()}} for the row-bound view,
\code{\link[=share_weights]{share_weights()}} for reaching a linked population through one frame

Other multiple frames:
\code{\link[=as.data.frame.frame_stack]{as.data.frame.frame_stack()}},
\code{\link[=as_svrepdesign.frame_stack]{as_svrepdesign.frame_stack()}},
\code{\link[=as_svydesign.frame_stack]{as_svydesign.frame_stack()}},
\code{\link[=declared_overlaps]{declared_overlaps()}},
\code{\link[=exante_overlaps]{exante_overlaps()}},
\code{\link[=exante_probabilities]{exante_probabilities()}},
\code{\link[=summary.frame_stack]{summary.frame_stack()}}
}
\concept{multiple frames}
