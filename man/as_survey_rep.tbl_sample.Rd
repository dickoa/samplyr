% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey.R
\name{as_survey_rep.tbl_sample}
\alias{as_survey_rep.tbl_sample}
\title{Convert a tbl_sample to a srvyr replicate-weight tbl_svy object}
\usage{
\method{as_survey_rep}{tbl_sample}(.data, ...)
}
\arguments{
\item{.data}{A \code{tbl_sample} object produced by \code{\link[=execute]{execute()}}.}

\item{...}{Additional arguments passed to \code{as_svrepdesign()}.}
}
\value{
A replicate-weight \code{tbl_svy} object from the srvyr package.
}
\description{
Creates a \link[srvyr:tbl_svy]{srvyr::tbl_svy} replicate design from a \code{tbl_sample} by first
converting to a \code{svyrep.design} object via \code{as_svrepdesign()},
then wrapping with \code{\link[srvyr:as_survey_rep]{srvyr::as_survey_rep()}}.
}
\examples{
\dontshow{if (requireNamespace("srvyr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(srvyr)

sample <- sampling_design() |>
  stratify_by(region, alloc = "proportional") |>
  draw(n = 300) |>
  execute(bfa_eas, seed = 42)

rep_tbl <- as_survey_rep(sample, type = "auto")
rep_tbl |>
  summarise(mean_hh = survey_mean(households, vartype = "se"))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{as_svrepdesign()} for survey replicate-weight export
}
