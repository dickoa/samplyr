% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/share-weights.R
\name{extend_links}
\alias{extend_links}
\title{Eliminate target clusters by extending links across them}
\usage{
extend_links(x)
}
\arguments{
\item{x}{A single column of \code{targets} naming the cluster. The column is
required: the extension is defined over the cluster, so it cannot be
expressed without one.}
}
\value{
\code{extend_links()} is only meaningful inside
\code{share_weights(within = )} and otherwise throws an error.
}
\description{
A declarative marker for \code{\link[=share_weights]{share_weights()}}'s \code{within} argument. It requests
Lavallee section 5.3: a source unit linked to any member of a cluster is
treated as linked to all of them, and target units are then treated as
their own clusters.
}
\details{
This is not a simplification of the clustered method. It produces a
different weight, because the extended structure counts source units
reaching a cluster where the clustered form counts links into it.
}
\examples{
# Used inside share_weights():
# share_weights(sample, targets, links, by, to,
#               within = extend_links(household),
#               multiplicity = complete_links())

}
\seealso{
Other weight sharing:
\code{\link[=complete_links]{complete_links()}},
\code{\link[=complete_weighted_links]{complete_weighted_links()}},
\code{\link[=share_weights]{share_weights()}},
\code{\link[=weighted_links]{weighted_links()}}
}
\concept{weight sharing}
