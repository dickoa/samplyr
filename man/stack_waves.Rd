% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stack-waves.R
\name{stack_waves}
\alias{stack_waves}
\title{Stack materialized waves into a long unit-wave table}
\usage{
stack_waves(...)
}
\arguments{
\item{...}{Two or more materialized waves, each from
\code{execute(master, wave = t)}, all of one master realization.}
}
\value{
A tibble with \code{wave}, \code{master_id}, \code{panel} and \code{design_weight}
first, then the columns of the waves themselves. One row per active
master row per wave: under with-replacement selection a population unit
may occupy several master rows, so a row is a selection occurrence rather
than necessarily a distinct population unit. \code{master_id} is unique within
a wave either way. It is an ordinary tibble, not a \code{tbl_sample}: it holds
several realizations and repeats the unit key on purpose.
}
\description{
Verifies that two or more materialized waves come from one executed master
and stacks them into a plain long table, one row per active master row per
wave. This is the structural handoff to an inference layer. Which estimator
of change is appropriate, and under what conditions, is a separate question
that depends on the design and the overlap. See
\code{vignette("rotating-panels")}.

Row-binding waves by hand does not reproduce the checks this performs. Two
executions of one design produce identical \code{.sample_id} values, so waves of
\emph{different} masters stack without complaint. A wave edited after
execution keeps a correct provenance record while its rows no longer match
it. Both are refused here.
}
\details{
\subsection{What the columns mean}{

Four columns are generated, and their names are chosen to prevent the two
substitutions that would quietly invalidate a downstream estimate:

\describe{
\item{\code{wave}}{The wave each row was observed at.}
\item{\code{master_id}}{A \strong{master-local} unit key, taken from \code{.sample_id}.
It matches units across waves of one master and means nothing outside
it. It is not a population identity, and it is not generally a primary
sampling unit: a clustered design's own cluster variable is carried
through unchanged and is what a consumer's \code{PSU} argument wants.}
\item{\code{panel}}{The rotation panel the row's \strong{assignment unit} was given,
or \code{NA} for a sample drawn without panels. That unit is the one the
master's \code{panel_stage} names, so several rows can share one panel: all
the members of a rotating household under a retained primary unit carry
its label, not one of their own.}
\item{\code{design_weight}}{samplyr's exact design weight for that wave,
including the activation factor. It is \strong{not} a final weight: nothing
here is adjusted for nonresponse or calibrated, so a consumer asking
for \code{w_final} is asking for something the analysis must supply.}
}

Every other column of each wave is carried through unchanged, including the
strata and cluster variables a consumer needs. samplyr's internal columns
are dropped, stage-specific quantities such as \code{.fpc_1} among them.
}

\subsection{What it does not do}{

It does not compute a covariance, a correlation, or a variance of change.
those belong to the inference layer, and which estimator is appropriate
depends on the overlap and the design. It does not reshape outcomes: attach
each wave's measurement under a common name before stacking. It does not
span cohorts of a \code{\link[=rotation_program]{rotation_program()}}, because identity across frame
vintages is not established.
}
}
\examples{
rotation <- data.frame(
  panel = rep(1:4, times = 4),
  wave = rep(1:4, each = 4),
  active = c(
    TRUE, TRUE, FALSE, FALSE,
    FALSE, TRUE, TRUE, FALSE,
    FALSE, FALSE, TRUE, TRUE,
    TRUE, FALSE, FALSE, TRUE
  )
)

master <- sampling_design() |>
  draw(n = 40) |>
  execute(bfa_eas, seed = 2025, panels = rotation)

stack_waves(execute(master, wave = 1), execute(master, wave = 2))

}
\seealso{
\code{\link[=execute]{execute()}} for materializing a wave, \code{\link[=as_svydesign]{as_svydesign()}} for the
per-wave export that supplies each wave's own variance,
\code{\link[=joint_expectation]{joint_expectation()}} with \code{waves} for how far two waves overlap

Other survey export:
\code{\link[=as_survey_design.tbl_sample]{as_survey_design.tbl_sample()}},
\code{\link[=as_survey_rep.tbl_sample]{as_survey_rep.tbl_sample()}},
\code{\link[=as_svrepdesign]{as_svrepdesign()}},
\code{\link[=as_svydesign]{as_svydesign()}}
}
\concept{survey export}
