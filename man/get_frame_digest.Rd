% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/digest.R
\name{get_frame_digest}
\alias{get_frame_digest}
\title{Read the frame digest of an executed sample}
\usage{
get_frame_digest(x)
}
\arguments{
\item{x}{A \code{tbl_sample} object produced by \code{\link[=execute]{execute()}}.}
}
\value{
The frame digest list, or \code{NULL} when none is recorded.
}
\description{
Low-level accessor to the frame digest that \code{execute()} records.
Returns \code{NULL} when the sample carries no digest. When a digest is
present, its effective status is computed lazily: the stored digest
is immutable, and a sample that no longer matches its executed
realization (per the sealed integrity record) reports status
\code{"invalidated"} without the digest itself being rewritten. Adding
analysis columns does not touch protected columns and therefore
does not invalidate.
}
\details{
This is an extension API for packages that build on the digest
(such as samplens, which draws it as a sampling card). The version
field of the stored digest is validated before returning. The list
structure of the returned digest is internal and versioned: it may
change between samplyr releases, so extension packages should pin a
minimum samplyr version. \code{\link[=frame_summary]{frame_summary()}} is the stable tabular
interface for everyone else.
}
\examples{
sample <- sampling_design() |>
  stratify_by(region) |>
  draw(n = 20) |>
  execute(bfa_eas, seed = 1)

digest <- get_frame_digest(sample)
names(digest)
digest$status

# NULL for anything that is not an executed sample
get_frame_digest(bfa_eas)
}
\seealso{
\code{\link[=frame_summary]{frame_summary()}}

Other extension APIs:
\code{\link{digest}},
\code{\link[=exante_digest]{exante_digest()}}
}
\concept{extension APIs}
