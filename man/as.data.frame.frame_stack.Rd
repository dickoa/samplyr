% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stack-frames.R
\name{as.data.frame.frame_stack}
\alias{as.data.frame.frame_stack}
\title{Row-bind a frame stack for inspection, not for estimation}
\usage{
\method{as.data.frame}{frame_stack}(x, ...)
}
\arguments{
\item{x}{A \code{frame_stack}.}

\item{...}{Not used.}
}
\value{
A data frame with \code{.frame} and \code{.domain} first, then the columns of
the components.
}
\description{
Returns a plain data frame with every component's rows, a \code{.frame} column
naming the component a row came from, and a \code{.domain} column naming the set
of frames the unit belongs to. It is the view to look at, join against, or
hand to another package.

It is deliberately not a \code{tbl_sample} and not a \code{frame_stack}. A unit
listed in two frames may appear on two rows carrying two different design
weights, so summing the weight column estimates nothing. Being a plain data
frame is what stops it reaching \code{\link[=as_svydesign]{as_svydesign()}} as though it were one
sample. Restoring a class attribute does not turn it back into a stack, and
modifying it does not touch the components.

\code{.domain} is a label for reading. Statistical code uses the membership
columns, which are carried through unchanged, and never parses it back.
}
\examples{
population <- data.frame(
  person_id = 1:60,
  in_landline = rep(c(TRUE, FALSE), times = c(40, 20)),
  in_cell = rep(c(FALSE, TRUE), times = c(10, 50))
)

frames <- stack_frames(
  landline = sampling_design() |>
    draw(n = 10) |>
    execute(population[population$in_landline, ], seed = 1),
  cell = sampling_design() |>
    draw(n = 12) |>
    execute(population[population$in_cell, ], seed = 2),
  membership = c(landline = "in_landline", cell = "in_cell"),
  key = person_id
)

view <- as.data.frame(frames)
table(view$.domain)

}
\seealso{
Other multiple frames:
\code{\link[=as_svrepdesign.frame_stack]{as_svrepdesign.frame_stack()}},
\code{\link[=as_svydesign.frame_stack]{as_svydesign.frame_stack()}},
\code{\link[=exante_overlaps]{exante_overlaps()}},
\code{\link[=exante_probabilities]{exante_probabilities()}},
\code{\link[=overlap_probabilities]{overlap_probabilities()}},
\code{\link[=overlap_weights]{overlap_weights()}},
\code{\link[=stack_frames]{stack_frames()}},
\code{\link[=summary.frame_stack]{summary.frame_stack()}}
}
\concept{multiple frames}
