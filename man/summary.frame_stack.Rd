% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{summary.frame_stack}
\alias{summary.frame_stack}
\title{Summarize a frame stack}
\usage{
\method{summary}{frame_stack}(object, ...)
}
\arguments{
\item{object}{A \code{frame_stack} produced by \code{\link[=stack_frames]{stack_frames()}}.}

\item{...}{Must be empty.}
}
\value{
Invisibly returns \code{object}. Called for its side effect of
printing a summary.
}
\description{
Reports the components, their membership columns and seeds, and how the
sampled rows fall across the domains: the frame sets units belong to. A
domain holding no rows is not shown, because absence from a sample is not
evidence that the domain is empty in the population.
}
\details{
The counts are rows, not distinct units. A unit listed in two frames and
selected from both appears twice, once per frame that selected it, which is
exactly the double count a composite weight has to resolve.
}
\examples{
population <- data.frame(
  person_id = 1:60,
  in_landline = rep(c(TRUE, FALSE), times = c(40, 20)),
  in_cell = rep(c(FALSE, TRUE), times = c(10, 50))
)

frames <- stack_frames(
  landline = sampling_design() |>
    draw(n = 10) |>
    execute(population[population$in_landline, ], seed = 1),
  cell = sampling_design() |>
    draw(n = 12) |>
    execute(population[population$in_cell, ], seed = 2),
  membership = c(landline = "in_landline", cell = "in_cell"),
  key = person_id
)

summary(frames)

}
\seealso{
Other multiple frames:
\code{\link[=as.data.frame.frame_stack]{as.data.frame.frame_stack()}},
\code{\link[=as_svrepdesign.frame_stack]{as_svrepdesign.frame_stack()}},
\code{\link[=as_svydesign.frame_stack]{as_svydesign.frame_stack()}},
\code{\link[=exante_overlaps]{exante_overlaps()}},
\code{\link[=exante_probabilities]{exante_probabilities()}},
\code{\link[=overlap_probabilities]{overlap_probabilities()}},
\code{\link[=overlap_weights]{overlap_weights()}},
\code{\link[=stack_frames]{stack_frames()}}
}
\concept{multiple frames}
