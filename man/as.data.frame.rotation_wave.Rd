% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rotation-program.R
\name{as.data.frame.rotation_wave}
\alias{as.data.frame.rotation_wave}
\title{Row-bind a rotation wave for fieldwork, not for estimation}
\usage{
\method{as.data.frame}{rotation_wave}(x, ...)
}
\arguments{
\item{x}{A rotation wave.}

\item{...}{Not used.}
}
\value{
A data frame.
}
\description{
The result is a plain data frame carrying a \code{.cohort} column, so it can be
used as the list of units to visit. It is deliberately not a \code{tbl_sample}:
the weights it carries are each cohort's own, the row set may hold a unit
once per cohort that drew it, and whether the cohorts overlap at all is
something the program does not know. Being a plain data frame is what
stops it reaching \code{\link[=as_svydesign]{as_svydesign()}} as though it were one sample.
}
